---
layout: base
---

{% assign previousPost = collections.post | getPreviousCollectionItem %}
{% assign nextPost = collections.post | getNextCollectionItem %}
{% comment %} <a onClick="history.back()">&lt;&lt; Back</a> {% endcomment %}

<section>
  <article>
    <header class="main">
      <h2>{{ title }}</h2>
      <p>{{ callout }}</p>
    </header>
    {% include "pretty-print.html" %}
      <br /><br />
      <strong>Published:</strong>&nbsp;
    {%- if timestamp %}
      {{ timestamp | readableTimestamp }}
    {%- else %}
      {{ date | readableDate }}
    {%- endif %}
    <br />
    {%- if isLocation%}
      {%- if visitDate %}
        <strong>Visit Date(s):</strong> {{ visitDate | readableDate }}<br />
      {% endif %}
      {%- if rating %}
        <strong>Rating:</strong> {{ rating }}<br />
      {% endif %}
      {%- if website %}
        <strong>Web site:</strong> <a href="{{ website }}" target="_blank">{{ website }}</a><br />
      {% endif %}
      {%- if address %}
        <strong>Address:</strong> <a href="https://www.google.com/maps/search/?api=1&query={{ address | url_encode }}" target="_blank">{{ address }}</a><br />
      {% endif %}
    {%- endif %}
    {%- if categories.length > 0 %}
      <strong>Found in</strong>{% for cat in categories %}
      <a href="/categories/{{ cat | slugify }}">{{ cat }}</a>
      {%- unless forloop.last %},{% endunless %}{%- endfor %}.
      <br />
    {% endif %}
    <br />
    {%- if description %}
      <blockquote>{{ description }}</blockquote>
    {%- endif %}
    {{ content }}
    </article>
    <hr />
    {% if nextPost %}
      <p>Next Post: <a href="{{ nextPost.url }}">{{ nextPost.data.title }}</a></p>
    {% endif %}
    {% if previousPost %} 
      <p>Previous Post: <a href="{{ previousPost.url }}">{{ previousPost.data.title }}</a>
  {% endif %}
</section>