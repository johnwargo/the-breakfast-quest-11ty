---
layout: base
---

{% comment %} <a onClick="history.back()">&lt;&lt; Back</a> {% endcomment %}
<section>
  <article>
    <header class="main">
      <h2>{{ title }}</h2>
      <p>{{ callout }}</p>
    </header>
    {% include "pretty-print.html" %}
    <br /><br />
    <strong>Published:</strong>&nbsp;
    {%- if timestamp %}
      {{ timestamp | readableTimestamp }}
    {%- else %}
      {{ date | readableDate }}
    {%- endif %}
    <br />
    {%- if isLocation%}
    <strong>Visit Date(s):</strong> {{ visitDates }}<br />
    <strong>Rating:</strong> {{ rating }}<br />
    <strong>Web site:</strong> <a href="{{ website }}" target="_blank">{{ website }}</a><br />
    <strong>Address:</strong> {{ address }}<br />
    {%- endif %}
    
    {%- if categories.length > 0 %}
      <br />
      <strong>Found in</strong>{% for cat in categories %}
        <a href="/categories/{{ cat | slugify }}">{{ cat }}</a>
        {%- unless forloop.last %},{% endunless %}{%- endfor %}.
        <br />
    {% endif %}
    <br />
    {%- if description %}      
      <blockquote>{{ description }}</blockquote>
    {%- endif %}    
    {{ content }}
  </article>
</section>
